providers:
  cursor:
    label: Cursor {model}
    command: ["agent", "-p", "--model", "{model}", "--yolo", "--trust", "{prompt}"]
  kilo:
    label: Kilo {model}
    command: ["kilo", "--model", "{model}", "run", "{prompt}"]

agents:
  - key: haiku
    label: Claude Haiku
    command: ["claude", "--model", "haiku", "-p", "{prompt}", "--dangerously-skip-permissions", "--output-format", "text"]
  - key: sonnet
    label: Claude Sonnet
    command: ["claude", "--model", "sonnet", "-p", "{prompt}", "--dangerously-skip-permissions", "--output-format", "text"]
  - key: opus
    label: Claude Opus
    command: ["claude", "--model", "opus", "-p", "{prompt}", "--dangerously-skip-permissions", "--output-format", "text"]
  - key: fable
    label: Claude Fable
    command: ["claude", "--model", "fable", "-p", "{prompt}", "--dangerously-skip-permissions", "--output-format", "text"]
  - key: claude
    label: Claude Sonnet
    command: ["claude", "--model", "sonnet", "-p", "{prompt}", "--dangerously-skip-permissions", "--output-format", "text"]
  - key: codex
    label: Codex
    command: ["codex", "exec", "--model", "gpt-5.6-sol", "-c", "model_reasoning_effort=\"medium\"", "--yolo", "{prompt}"]
    review_command: ["codex", "exec", "--model", "gpt-5.6-sol", "-c", "model_reasoning_effort=\"medium\"", "review", "--uncommitted", "{prompt}"]
  - key: antigravity
    label: Antigravity
    command: ["agy", "--print", "{prompt}", "--dangerously-skip-permissions"]
  - key: kilo
    label: kilo/kilo-auto/free
    command: ["kilo", "--model", "kilo/kilo-auto/free", "run", "{prompt}"]
  - key: kilo-opus-4.6
    label: Kilo Claude Opus 4.6
    command: ["kilo", "--model", "kilo/anthropic/claude-opus-4.6", "run", "{prompt}"]
  - key: kilo-opus-4.7
    label: Kilo Claude Opus 4.7
    command: ["kilo", "--model", "kilo/anthropic/claude-opus-4.7", "run", "{prompt}"]
  - key: kilo-sonnet-4.6
    label: Kilo Claude Sonnet 4.6
    command: ["kilo", "--model", "kilo/anthropic/claude-sonnet-4.6", "run", "{prompt}"]
  - key: cursor-auto
    label: Cursor Auto
    command: ["agent", "-p", "--model", "auto", "--yolo", "--trust", "{prompt}"]
  - key: cursor-composer-2.5
    label: Cursor Composer 2.5
    command: ["agent", "-p", "--model", "composer-2.5", "--yolo", "--trust", "{prompt}"]
  - key: cursor-grok-4.5
    label: Cursor Grok 4.5
    command: ["agent", "-p", "--model", "cursor-grok-4.5-high", "--yolo", "--trust", "{prompt}"]
  - key: cursor-opus-4.6
    label: Cursor Opus 4.6
    command: ["agent", "-p", "--model", "claude-4.6-opus-high", "--yolo", "--trust", "{prompt}"]
  - key: cursor-opus-4.7
    label: Cursor Opus 4.7
    command: ["agent", "-p", "--model", "claude-opus-4-7-high", "--yolo", "--trust", "{prompt}"]
  - key: cursor-sonnet-4.6
    label: Cursor Sonnet 4.6
    command: ["agent", "-p", "--model", "claude-4.6-sonnet-medium", "--yolo", "--trust", "{prompt}"]
